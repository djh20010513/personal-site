# 🚀 部署到 Vercel 永久网址 · 零基础教程

整个流程 **5-10 分钟**。我会一步步带你做。

---

## 第 1 步：注册 GitHub 账号（2 分钟）

如果你已经有 GitHub 账号，跳过这步。

1. 打开 https://github.com/signup
2. 填邮箱、密码、用户名（建议用真名或简单英文）
3. 验证邮箱
4. 选 Free 免费版即可

---

## 第 2 步：建一个仓库（1 分钟）

1. 登录 GitHub 后打开 https://github.com/new
2. **Repository name** 填：`personal-site`（或你喜欢的名字）
3. 选 **Public**（公开）
4. **不要**勾选 "Add a README file"（我们后面会推上来）
5. 点 **Create repository**
6. **复制** 仓库地址（形如 `https://github.com/你的用户名/personal-site.git`），备用

---

## 第 3 步：把代码推上去（2 分钟）

打开 Mac 终端（按 `Cmd + 空格` 搜"终端"），依次执行：

```bash
cd /Users/djh/personal-site

git init
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/你的用户名/personal-site.git
git push -u origin main
```

> ⚠️ 把 `你的用户名` 替换成你 GitHub 上真实的用户名。

第一次推送会让你登录 GitHub，按提示输入用户名密码（或 Personal Access Token）。

---

## 第 4 步：导入 Vercel（2 分钟）

1. 打开 https://vercel.com/signup
2. 点 **Continue with GitHub** 用 GitHub 登录
3. 登录后会自动跳到 dashboard，点右上角 **Add New → Project**
4. 在 "Import Git Repository" 列表里找到你刚创建的 `personal-site`
5. 点 **Import**
6. 保持所有默认设置，**直接点 Deploy**（不要改任何东西）

---

## 第 5 步：拿到你的永久网址 ✅

Vercel 会自动：
- 拉取你的代码
- 安装依赖
- 构建网站
- 部署到全球 CDN

整个过程约 **60-90 秒**。完成后你会看到 🎉 撒花页面，顶部显示：

```
https://personal-site-你的用户名.vercel.app
```

**这就是你的永久网址！** 任何人任何时候都能打开。

---

## 📌 常见问题

### Q: 后续修改内容怎么办？
修改 `lib/data.ts` 后：
```bash
cd /Users/djh/personal-site
git add .
git commit -m "update content"
git push
```
Vercel 会**自动重新部署**，1 分钟内网址就更新了。

### Q: 想绑定自己的域名（比如 yourname.com）？
1. 在 Vercel 项目页 → Settings → Domains
2. 输入你的域名
3. 去你买域名的网站（比如阿里云万网、Namecheap）添加 CNAME 记录指向 `cname.vercel-dns.com`
4. 等 5-30 分钟生效

### Q: 部署失败了？
在 Vercel 项目页 → Deployments → 点失败的部署 → 看 Build Logs
最常见原因：
- TypeScript 报错 → 本地先 `npm run build` 跑一遍
- 依赖装不上 → 检查 `package.json`

### Q: 完全不懂 git 怎么办？
如果你不想用 git 命令行，告诉我你的 GitHub 用户名，我帮你重新写一个**不用 git、纯网页上传文件**的方案。

---

## 💡 部署后的优化建议

1. **替换内容**：打开 `lib/data.ts`，把你的真实姓名、项目、文章填进去
2. **添加图片**：把头像放 `public/avatar.jpg`，项目封面放 `public/projects/`
3. **修改后推送**：`git push` 一行命令自动部署
4. **绑定域名**：在 Vercel 控制台一键搞定

---

## 🎁 额外福利

- 自动 HTTPS（绿色小锁）
- 全球 CDN（中国访问也快）
- 每次 push 自动部署
- 免费额度永久够用（个人网站）

---

**遇到任何问题，把错误截图发给我，我帮你 debug！** 🚀